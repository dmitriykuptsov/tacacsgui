group = ##group_name {
    valid until = ##valid_until
    ##enable_password
    ##default_service
    ##access
    service = shell {
        default cmd = ##default_cmd
        set priv-lvl = ##privilege_level
        ##cmds
    }
}
